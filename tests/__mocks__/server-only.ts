// Stub for Next.js "server-only" package.
// In production Next.js builds, this module throws if imported in a
// client component. In the Vitest Node environment we just export nothing —
// the guard is not needed and the repository logic under test is what matters.
export {};
